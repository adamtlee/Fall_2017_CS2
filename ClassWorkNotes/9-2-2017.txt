class Student {
  sid int;
  name String;
  major String;
}

Student Fred = new Student('Fred', 42, 'CS');
Student Barney = new Student('Barney', 99, 'MTH');
if (Fred == Barney){
  in Object
    CompareTo
}

if (Fred.compareTo(Barney) == 0){

}

public int myCompare(class?){
  (Student n, Student x) {
    if(n.name == x.name &&
    n.sid == x.sid) &&
    n.major == major) return 0;

    else return -1;
  }
}

// HW 3 Tips:

import java.util.*;

class Test {
  class Countries {
    countryName String;
  }
  ArrayList<object> thing = new ArrayList<object>();

  ArrayList<Countries> thing = new ArrayList<Contries>();

  thing.countryName = 'Germany'; // won't work

  thing.add("Germany");

  thing.add(0,"France");

  thing.add(99, "Italy");

}
